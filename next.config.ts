import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['fpoimg.com', 'lh3.googleusercontent.com', '127.0.0.1', 'services-eu1.arcgis.com', 'via.placeholder.com', 'www.inavi.com.uy', 'static.wixstatic.com'],
  },
};


export default nextConfig;
